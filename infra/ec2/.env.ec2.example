# App secrets
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI=https://api.example.com/auth/strava/callback
STRAVA_SCOPES=read,activity:read_all
AUTH_SUCCESS_REDIRECT_URL=/

# App runtime
APP_HOST=0.0.0.0
APP_PORT=8000
WEB_CONCURRENCY=2
LOG_LEVEL=info
GUNICORN_TIMEOUT=60
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.0

# Compose networking (keep private and expose through Nginx)
HOST_API_BIND=127.0.0.1
HOST_API_PORT=8000
HOST_DB_BIND=127.0.0.1
HOST_REDIS_BIND=127.0.0.1

# DB connection used by API container
DATABASE_URL=postgresql+psycopg://app:app@db:5432/livemap
